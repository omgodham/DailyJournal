<%- include('header') %>

<h1>Home</h1>
<p><%= ejsHomeStartingContent %></p>
    <% ejsHomePosts.forEach(function(item){%>
        <h1><%= item.title %></h1>   
        <p><%=item.post.substring(0,100) %>
            <a href="/posts/<%= item.title %>">Read More</a></p>
    <% }) %>
<%- include('footer') %>